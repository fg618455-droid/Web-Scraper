# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# Database
*.db
*.db-shm
*.db-wal

# Logs
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Linux-Filesystem Artefakte (Cowork/WSL)
.fuse_hidden*
.claude/

# App-generated
icon.ico
icon.png
*.lnk
debug_html/
playwright-check/
.playwright/
version.json

# Local scratch dir: contains Playwright browser bundles (large) + ad-hoc
# diagnosis scripts. We DO want the .py scripts in the repo, but NOT the
# binaries that Playwright drops here when run from this folder.
play/*
!play/*.py
!play/*.md

# Config (falls Secrets drin landen)
.env
# eBay-Login-Cookies (Iter. 25) — niemals committen
ebay_session.json

# Build-Spec — alle ausser DealScraper.spec (vom CI gebraucht)
*.spec
!DealScraper.spec
